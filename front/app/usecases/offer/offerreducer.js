const InitialState = {
  offers: [],
  detailoffer: null,
  loading: false,
  searching: false,
  searchingState : {},
};

export default offerState = (state = InitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
